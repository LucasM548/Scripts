#!/bin/bash
# commande lkt trash
if [ ! -d ~/Bureau/trash ], then
    echo "Le répertoire "~/Bureau/trash" n'existe pas."
    else
        echo "Contenu de trash :"
        ls -l ~/Bureau/trash/* 2> /dev/null
    if [ $? -ne 0 ] ; then
        echo répertoire trash vide;
    fi
fi